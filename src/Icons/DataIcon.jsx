import React from 'react';

const DataIcon = ({ width, height }) => {
    return (
        <img 
            src={`./DataIcon.png`} 
            alt="Data Icon" 
            width={width} 
            height={height} 
        />
    );
};

export default DataIcon;